MBC Inventory Build Repair V1.5.5

Static TypeScript verification: PASSED
Files checked include:
- all app/*.tsx pages in the working project
- app/api/admin/users/route.ts
- components/*.tsx
- lib/*.ts

Important limitation:
The execution environment could not access the npm registry, so a full npm install / next build was not run locally.
The exact TypeScript errors visible in the Vercel logs and the subsequent known type errors were corrected and checked with strict TypeScript settings.

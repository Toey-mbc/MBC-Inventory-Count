'use client'
import {useEffect,useState} from 'react';import Protected from '@/components/Protected';import {createClient} from '@/lib/supabase/client'
export default function Page(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{createClient().from('unknown_barcodes').select('*').order('last_seen_at',{ascending:false}).then(({data})=>setRows(data||[]))},[]);return <Protected><div className="page-head"><div><div className="page-title">บาร์โค้ดไม่พบ</div><div className="muted">ผูกสินค้า เพิ่มสินค้าใหม่ หรือข้ามรายการค้าง</div></div></div><div className="card table-wrap"><table className="table"><thead><tr><th>บาร์โค้ด</th><th>จำนวนครั้ง</th><th>สถานะ</th><th>พบล่าสุด</th></tr></thead><tbody>{rows.length?rows.map(x=><tr key={x.id}><td><b>{x.barcode}</b></td><td>{x.scan_count||x.quantity||1}</td><td><span className="badge">{x.status}</span></td><td>{x.last_seen_at?new Date(x.last_seen_at).toLocaleString('th-TH'):'-'}</td></tr>):<tr><td colSpan={4} className="empty-state">ยังไม่มีบาร์โค้ดไม่พบ</td></tr>}</tbody></table></div></Protected>}

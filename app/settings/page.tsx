import Protected from '@/components/Protected'
export default function Settings(){return <Protected><div className="topbar"><div><div className="page-title">ตั้งค่าและล้างข้อมูลทดสอบ</div><div className="muted">ดำเนินการผ่าน SQL function ที่ตรวจสิทธิ์ Admin</div></div></div><div className="card"><h3>การล้างข้อมูล UAT</h3><p>เปิด Supabase SQL Editor แล้วเรียก <code>select public.reset_test_data();</code> ด้วยบัญชีฐานข้อมูล หรือพัฒนา UI Admin ต่อจาก RPC นี้ ข้อมูล Master และบัญชีผู้ใช้จะยังอยู่</p><div className="notice">ก่อนล้างข้อมูล ตรวจสอบว่า Offline Queue ของทุกเครื่องเป็น 0 และ Export รายงานที่ต้องการเก็บแล้ว</div></div></Protected>}

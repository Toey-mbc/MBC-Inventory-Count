'use client'
import {useEffect,useState} from 'react';import Protected from '@/components/Protected';import {createClient} from '@/lib/supabase/client'
export default function Page(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{createClient().from('scan_totals').select('*,products(sku,name),warehouses(code,name),locations(code,name)').order('updated_at',{ascending:false}).then(({data})=>setRows(data||[]))},[]);return <Protected><div className="page-head"><div><div className="page-title">สินค้าคงคลัง</div><div className="muted">ยอดนับล่าสุดแยกตามสินค้า คลัง โลเคชั่น และสภาพ</div></div></div><div className="card table-wrap"><table className="table"><thead><tr><th>SKU</th><th>สินค้า</th><th>คลัง</th><th>โลเคชั่น</th><th>สภาพ</th><th>จำนวน</th></tr></thead><tbody>{rows.length?rows.map(x=><tr key={x.id}><td><b>{x.products?.sku||'-'}</b></td><td>{x.products?.name||'-'}</td><td>{x.warehouses?.code||'-'}</td><td>{x.locations?.code||'-'}</td><td><span className="badge">{x.condition}</span></td><td><b>{x.quantity}</b></td></tr>):<tr><td colSpan={6} className="empty-state">ยังไม่มีข้อมูลยอดตรวจนับ</td></tr>}</tbody></table></div></Protected>}

# แก้ไขรายงาน V1.6.1

## สาเหตุ

ฐานข้อมูลที่ใช้งานมีตาราง `locations` จากโครงสร้างเริ่มต้น ซึ่งยังไม่มีคอลัมน์ `zone` แต่ไฟล์รายงาน V1.6.0 เรียกคอลัมน์นี้ทันที จึงสร้าง View ไม่สำเร็จ

## วิธีแก้

1. เปิด Supabase > SQL Editor
2. เปิดไฟล์ `RUN_THIS_SQL_V1_6_1.sql`
3. คัดลอก SQL ทั้งหมดไปวางแล้วกด Run
4. ต้องเห็นผล `Success. No rows returned`
5. กลับหน้าเว็บและกดรีเฟรช

SQL ชุดนี้เพิ่มฟิลด์ที่ขาด, สร้าง Trigger อัปเดตเวลา และสร้าง `inventory_count_report` ใหม่ สามารถรันซ้ำได้และไม่ลบข้อมูลตรวจนับ

หน้า Reports V1.6.1 ยังมีโหมดสำรอง: หาก View ยังไม่พร้อม ระบบจะอ่านจากตารางหลัก และหาก `zone` ยังไม่มีจะแสดง `-` แทน ไม่ทำให้หน้ารายงานล่ม
